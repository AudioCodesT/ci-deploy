---
- name: Installes NFS server for SBC project
  hosts: sbc-nfs
  sudo: true
  vars:
    - ansible_ssh_user: centos
    - ansible_ssh_private_key_file: ~/.ssh/AudioCodes-CICD-Management.pem
  roles:
     - dns
     - geerlingguy.git
     - geerlingguy.repo-epel
     - nfs
